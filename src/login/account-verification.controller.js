AccountVerificationCtrl.$inject = ['$state', 'LoginService'];

export default function AccountVerificationCtrl($state, LoginService) {
  /*jshint validthis: true */
  var vm = this;
  vm.pageLoaded = false;
}
