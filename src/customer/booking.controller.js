BookingCtrl.$inject = ['$state','localStorageService','$scope','$document'];

export default function BookingCtrl($state, localStorageService, $scope, $document) {
  /*jshint validthis: true */
  var vm = this;
  vm.pageLoaded = false;
  
  
  
}
